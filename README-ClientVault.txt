Adds Client Vault tab + access code UX (static), ties access code into proposal generator, adds Best-for labels on pricing, and adds Back-to-Hub links.
To automate vault delivery: connect Google Apps Script or GCP (Cloud Function) to verify deposit and issue signed links.
